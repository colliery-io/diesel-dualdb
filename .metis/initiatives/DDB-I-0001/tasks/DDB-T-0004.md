---
id: pooling-check-bool-native-shim
level: task
title: "Pooling check + Bool/native-shim findings + spike report"
short_code: "DDB-T-0004"
created_at: 2026-06-08T12:50:33.130150+00:00
updated_at: 2026-06-08T15:12:22.394824+00:00
parent: DDB-I-0001
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: DDB-I-0001
---

# Pooling check + Bool/native-shim findings + spike report

## Parent Initiative

[[DDB-I-0001]]

## Objective

Close out the spike: confirm pooling works over `DualConnection`, answer the two scope-shaping questions (is Bool already supported? is a native-type shim feasible?), and — the primary deliverable — write the **spike report** back into the initiative ([[DDB-I-0001]]) so M0 and M1 can be designed from fact rather than assumption.

## Acceptance Criteria

## Acceptance Criteria

- [x] **Pooling:** r2d2 `Pool` over `DualConnection` via the derive's `R2D2Connection` impl (no manual manager needed) acquires a connection and runs a real query — bridge round-trip through pooled conns on both backends (`tests/pool.rs`).
- [x] **Bool question answered:** `MultiBackend` already satisfies `HasSqlType<Bool>` (core set). ⇒ **`types::Bool` dropped from M0 scope.**
- [x] **Native-shim opinion:** feasible — mechanically identical (helpers generic over `ST`); risk is import shadowing + SQLite repr choice, not expressibility. Deferred.
- [x] **Spike report** written into [[DDB-I-0001]] with exact working impl signatures, the helper-based dispatch (not raw RawValue matching), the `returning_clauses_for_sqlite_3_35` gotcha, and a clear **GO**.

## Implementation Notes

### Technical Approach
This task is mostly investigation + writing. The pooling check is small once [[DDB-T-0003]] proved query execution. The report is the artifact every downstream initiative reads, so be concrete: paste the working impl signatures, not prose summaries.

### Dependencies
[[DDB-T-0003]] (the bridge must be working — or definitively not — before the report can conclude GO/NO-GO).

### Risk Considerations
If the bridge succeeded, this is low-risk wrap-up. If it failed, this task's report becomes the pivot document: document the failure precisely and recommend the architecture change (upstream patch, alternative abstraction) so the M0/M1 initiatives can be re-planned.

## Status Updates

### 2026-06-08 — Complete

Spike closed out. Pooling proven; all scope questions answered; consolidated **GO** report written into [[DDB-I-0001]].

- **Pooling** (`tests/pool.rs`): `Pool::builder().build(ConnectionManager::<DualConnection>::new(url))` → `pool.get()` → bridge insert + filtered select, both backends green. The derive's `R2D2Connection` impl is sufficient; no hand-written manager needed.
- **Bonus finding:** the derived `Connection::establish` tries Pg first then Sqlite (no scheme detection) — recorded in the report as justification for M2's `Pool::connect`.
- Bool / native-shim / report: see the AC and the SPIKE REPORT section of [[DDB-I-0001]].

All 8 tests pass under `angreal test all`. Spike initiative is ready to close at the human checkpoint; M0 ([[DDB-I-0002]]) inherits: drop Bool, make `returning_clauses_for_sqlite_3_35` required, verify MSRV.