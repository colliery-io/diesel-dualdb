---
id: scaffold-diesel-dualdb-macros
level: task
title: "Scaffold diesel-dualdb-macros workspace crate"
short_code: "DDB-T-0011"
created_at: 2026-06-09T01:11:14.583430+00:00
updated_at: 2026-06-09T01:18:46.546200+00:00
parent: DDB-I-0003
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: DDB-I-0003
---

# Scaffold diesel-dualdb-macros workspace crate

## Parent Initiative

[[DDB-I-0003]]

## Objective

Convert the single-crate project into a Cargo workspace and add an empty `diesel-dualdb-macros` proc-macro crate, so the macros ([[DDB-T-0012]], [[DDB-T-0013]]) have a home. No macro logic yet — just structure that builds and keeps the existing 20 tests green.

## Acceptance Criteria

## Acceptance Criteria

- [x] Root `Cargo.toml` has `[workspace] members = ["diesel-dualdb-macros"]`, `resolver = "2"`; `diesel-dualdb` stays the root package.
- [x] `diesel-dualdb-macros/` crate: `[lib] proc-macro = true`, deps syn 2 (full) / quote 1 / proc-macro2 1; `src/lib.rs` compiles (doc-only scaffold).
- [~] `diesel-dualdb` → `diesel-dualdb-macros` path dependency + re-export — **deferred to [[DDB-T-0012]]**: with no macro yet, an unused path dep would be dead weight; wired when `#[dualdb::test]` exists.
- [x] `cargo build --workspace`, `angreal test all` (20 tests), `angreal check all` all green.
- [x] Feature matrix sane under the workspace (root-package scoped). **Improved `angreal check clippy` to `--workspace`** so the macros crate is linted too.

## Implementation Notes

### Technical Approach
Keep `diesel-dualdb` as the workspace root package and add `[workspace] members = ["diesel-dualdb-macros"]` (root package is implicitly a member). Pin syn 2 / quote 1 / proc-macro2 1. Decide whether the macros crate is always-on or feature-gated by the lib — likely always a dependency (the macros are cheap and gated by *their own* generated cfg). Workspace `resolver = "2"`.

### Dependencies
First M1 task; blocks [[DDB-T-0012]] and [[DDB-T-0013]]. No blockers itself.

### Risk Considerations
Low. Watch: angreal tasks run cargo from root — after workspacing, `cargo test` runs the whole workspace (fine). Make sure the proc-macro crate doesn't accidentally pull the heavy diesel deps.

## Status Updates

### 2026-06-08 — Complete

Workspace + empty proc-macro crate in place.

**Files:** root `Cargo.toml` (`[workspace]`), `diesel-dualdb-macros/{Cargo.toml,src/lib.rs}`, `.angreal/task_check.py` (clippy → `--workspace`).

**Verification:** `cargo build --workspace` builds both crates; `angreal test all` 20 green; `angreal check all` clean (fmt/clippy-workspace/feature-matrix).

**Decision:** held off wiring the `diesel-dualdb → diesel-dualdb-macros` dependency + re-export until [[DDB-T-0012]], to avoid an unused-dependency until there's an actual macro. The macros crate is otherwise ready.

**Handoff to [[DDB-T-0012]]:** add `diesel-dualdb-macros = { path = "diesel-dualdb-macros" }` to `[dependencies]`, implement `#[proc_macro_attribute] pub fn test`, and `pub use diesel_dualdb_macros::test;` from the lib.