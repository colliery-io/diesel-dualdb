---
id: v1-release-polish-rustdoc-metadata
level: task
title: "v1 release polish (rustdoc, metadata, CHANGELOG, checklist)"
short_code: "DDB-T-0023"
created_at: 2026-06-09T22:47:01.921185+00:00
updated_at: 2026-06-10T01:45:17.512198+00:00
parent: DDB-I-0004
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: DDB-I-0004
---

# v1 release polish (rustdoc, metadata, CHANGELOG, checklist)

## Parent Initiative

[[DDB-I-0004]]

## Objective

Get the crate publish-ready as v1 (sync): fill the metadata gaps, a rustdoc pass, a CHANGELOG, and a release checklist. The last task of M2 and of v1.

## Acceptance Criteria

## Acceptance Criteria

- [x] **Metadata:** `diesel-dualdb` + `diesel-dualdb-macros` → version **0.1.0**, publishable (removed `publish = false`), keywords/categories/license/readme set; the lib's macros dep is now `{ path, version = "0.1.0" }` (publishable, not path-only). `schema-gen` stays `publish = false` (dev tool). `repository`/`homepage` left as `TODO(release)` — needs the git remote.
- [x] **Rustdoc:** `RUSTDOCFLAGS="-D warnings" cargo doc --all-features --no-deps --workspace` builds clean (fixed one redundant-link warning in `pool.rs`). Crate docs link to `docs/`.
- [x] **README:** added a "Connecting" (`Pool::connect`) snippet; features/MSRV/schema-gen/test-macro sections + `docs/` link confirmed.
- [x] **CHANGELOG.md** — 0.1.0 entry.
- [x] **RELEASING.md** — publish order (macros → lib), pre-flight checks, the dry-run caveat (lib can't fully dry-run until macros is on crates.io), remote-dependent TODOs.
- [x] `angreal check all` + `angreal test all` green; doc clean; schema fresh.
- [~] **`cargo publish --dry-run`:** macros packages cleanly **after commit** (the only failure was the dirty working tree); the lib dry-run is blocked until macros is published (documented in RELEASING.md). The `no repository` warning is expected (remote pending).

**Remote-gated (documented in RELEASING.md, not done):** `repository`/`homepage`/`documentation` URLs, CI badge, branch protection, the actual `cargo publish`.

## Implementation Notes

### Technical Approach
Mechanical. The blocker on some items is the **GitHub remote** (repository URL, badge, branch protection) — do the metadata that doesn't need it, and list the remote-dependent steps in the checklist. Publish order: `diesel-dualdb-macros` first (the lib path-depends on it; for a real publish it needs a version dep, not just path). Decide `schema-gen`'s fate (likely `publish = false` or a separate release).

### Dependencies
Last M2 task — do after the Pool/returning/escape tasks so their public API is in the rustdoc pass. Some items gated on the repo having a remote.

### Risk Considerations
Low. The path-vs-version dependency on `diesel-dualdb-macros` is the one real publish gotcha (a path-only dep can't be published) — switch to `{ version = "…", path = "…" }`.

## Status Updates

### 2026-06-10 — Complete — v1 release-ready (modulo the remote)

Metadata, rustdoc, CHANGELOG, and a release checklist are in.

**Files:** `Cargo.toml` (lib 0.1.0, publishable, macros dep `version="0.1.0"`), `diesel-dualdb-macros/Cargo.toml` (0.1.0, publishable, keywords/categories), `src/pool.rs` (doc-link fix), `CHANGELOG.md`, `RELEASING.md`, `README.md` (Connecting section), `docs/` (index status + pool how-to + design-philosophy roadmap updated to reflect Pool/dispatch shipped).

**Verified:** `angreal check all` clean; `angreal test all` all green; `cargo doc -D warnings` clean; `angreal schema check` fresh. `cargo publish --dry-run -p diesel-dualdb-macros` packages once the tree is committed.

**Blocked on a git remote (in RELEASING.md):** `repository`/`homepage` URLs, CI badge, branch protection, and the actual publish. The lib's full publish dry-run also waits on the macros crate being on crates.io.

**M2 complete** — all four tasks done. v1 (sync) is feature-complete and release-ready pending a remote.