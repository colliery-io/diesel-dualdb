# Code Index

> Generated: 2026-06-11T14:37:31Z | 27 files | Rust

## Project Structure

```
├── diesel-dualdb-cli/
│   └── src/
│       ├── lib.rs
│       └── main.rs
├── diesel-dualdb-macros/
│   └── src/
│       └── lib.rs
├── schema/
│   └── generated/
│       └── schema.rs
├── src/
│   ├── backend.rs
│   ├── escape.rs
│   ├── lib.rs
│   ├── pool.rs
│   ├── sql_types.rs
│   └── types/
│       ├── array.rs
│       ├── bytes.rs
│       ├── decimal.rs
│       ├── json.rs
│       ├── mod.rs
│       ├── timestamp.rs
│       └── uuid.rs
└── tests/
    ├── array.rs
    ├── bytes.rs
    ├── connectivity.rs
    ├── decimal.rs
    ├── enum.rs
    ├── generated.rs
    ├── json.rs
    ├── pool.rs
    ├── timestamp.rs
    ├── upsert.rs
    └── uuid.rs
```

## Modules

### diesel-dualdb-cli/src

> *Semantic summary to be generated by AI agent.*

#### diesel-dualdb-cli/src/lib.rs

- pub `Result` type L42 — `= std::result::Result<T, String>` — ```
- pub `Backend` enum L45-48 — `Postgres | Sqlite` — ```
- pub `Mapped` struct L52-57 — `{ pg: &'static str, sqlite: &'static str, marker: &'static str }` — One logical type's three renderings.
- pub `map_type` function L70-94 — `(dt: &DataType) -> Option<Mapped>` — The mapping table: logical SQL type → (PG native, SQLite native, marker).
- pub `Migration` struct L497-501 — `{ dir_name: String, up_sql: String, down_sql: Option<String> }` — ```
- pub `read_migrations` function L503-540 — `(dir: &Path) -> Result<Vec<Migration>>` — ```
- pub `RenderedMigration` struct L545-549 — `{ dir_name: String, up_sql: String, down_sql: String }` — ```
- pub `Generated` struct L552-556 — `{ postgres: Vec<RenderedMigration>, sqlite: Vec<RenderedMigration>, schema_rs: S...` — ```
- pub `generate` function L558-597 — `(migrations: &[Migration]) -> Result<Generated>` — ```
- pub `write_outputs` function L599-612 — `(g: &Generated, out_dir: &Path) -> Result<()>` — ```
-  `Mapped` type L59-66 — `= Mapped` — ```
-  `native` function L60-65 — `(&self, backend: Backend) -> &'static str` — ```
-  `native_data_type` function L96-102 — `(dt: &DataType, backend: Backend, ctx: &str) -> Result<DataType>` — ```
-  `last_ident` function L105-110 — `(name: &ObjectName) -> String` — Last identifier of a (possibly schema-qualified) object name.
-  `ModelColumn` struct L114-118 — `{ name: String, marker: &'static str, nullable: bool }` — ```
-  `ModelTable` struct L120-126 — `{ name: String, columns: Vec<ModelColumn>, primary_key: Vec<String>, foreign_key...` — ```
-  `ModelTable` type L128-139 — `= ModelTable` — ```
-  `primary_key_cols` function L129-138 — `(&self) -> Vec<String>` — ```
-  `Model` struct L142-144 — `{ tables: Vec<ModelTable> }` — ```
-  `Model` type L146-302 — `= Model` — ```
-  `table_mut` function L147-149 — `(&mut self, name: &str) -> Option<&mut ModelTable>` — ```
-  `apply` function L152-268 — `(&mut self, stmt: &Statement) -> Result<()>` — Fold a parsed statement into the model (the `schema.rs` source of truth).
-  `render_schema_rs` function L270-301 — `(&self) -> String` — ```
-  `render_use` function L304-310 — `(path: &str, names: &[&str]) -> Option<String>` — ```
-  `marker_parts` function L312-315 — `(marker: &str) -> (&str, &str)` — ```
-  `render_table_macro` function L317-361 — `(t: &ModelTable) -> String` — ```
-  `parse` function L365-367 — `(sql: &str) -> Result<Vec<Statement>>` — ```
-  `rewrite_types` function L370-393 — `(stmt: &mut Statement, backend: Backend) -> Result<()>` — Rewrite a statement's column types to `backend`'s native types in place.
-  `render_up` function L396-413 — `(segments: &[Segment], backend: Backend) -> Result<String>` — Render the `up.sql` for one backend from the migration's segments.
-  `derive_down` function L417-434 — `(segments: &[Segment]) -> Result<String>` — `DROP TABLE` for each table created in this migration's shared segments, in
-  `Segment` enum L438-441 — `Shared | Tagged` — ```
-  `segment` function L444-493 — `(sql: &str) -> Result<Vec<Segment>>` — Split logical DDL into shared chunks and per-backend tagged blocks.
-  `write` function L614-616 — `(path: &Path, contents: &str) -> Result<()>` — ```
-  `tests` module L619-716 — `-` — ```
-  `mig` function L622-628 — `(name: &str, up: &str) -> Migration` — ```
-  `fk_composite_pk_index_and_escape_hatch` function L631-701 — `()` — ```
-  `unmapped_type_is_an_error` function L704-708 — `()` — ```
-  `unclosed_escape_block_errors` function L711-715 — `()` — ```

#### diesel-dualdb-cli/src/main.rs

-  `main` function L12-29 — `() -> ExitCode` — `<out_dir>`.
-  `run` function L31-39 — `( migrations_dir: &std::path::Path, out_dir: &std::path::Path, ) -> diesel_duald...` — `<out_dir>`.

### diesel-dualdb-macros/src

> *Semantic summary to be generated by AI agent.*

#### diesel-dualdb-macros/src/lib.rs

- pub `test` function L35-93 — `(attr: TokenStream, item: TokenStream) -> TokenStream` — Run one test body against each backend.
- pub `bridge` function L112-140 — `(input: TokenStream) -> TokenStream` — Generate the `MultiBackend` bridge impls for a **non-generic** portable type.
- pub `dual_enum` function L168-174 — `(input: TokenStream) -> TokenStream` — Derive a portable enum: a fieldless Rust enum that maps to a PostgreSQL
-  `dual_enum_impl` function L176-370 — `(input: DeriveInput) -> syn::Result<proc_macro2::TokenStream>` — this crate directly.
-  `Bridge` struct L373-376 — `{ sql: Path, rust: Path }` — `bridge!(Marker, Newtype)` input.
-  `Bridge` type L378-387 — `impl Parse for Bridge` — this crate directly.
-  `parse` function L379-386 — `(input: ParseStream) -> syn::Result<Self>` — this crate directly.
-  `parse_backends` function L390-404 — `(attr: TokenStream) -> syn::Result<(bool, bool)>` — Parse the attribute arguments into `(pg, sqlite)` selections.

### src

> *Semantic summary to be generated by AI agent.*

#### src/backend.rs

-  `json` module L54-91 — `-` — behind the same feature gate as the type itself.
-  `MultiBackend` type L64-68 — `= MultiBackend` — behind the same feature gate as the type itself.
-  `metadata` function L65-67 — `(lookup: &mut Self::MetadataLookup) -> Self::TypeMetadata` — behind the same feature gate as the type itself.
-  `to_sql` function L77-80 — `(&'b self, out: &mut Output<'b, '_, MultiBackend>) -> serialize::Result` — behind the same feature gate as the type itself.
-  `from_sql` function L87-89 — `(bytes: <MultiBackend as Backend>::RawValue<'_>) -> deserialize::Result<Self>` — behind the same feature gate as the type itself.

#### src/escape.rs

- pub `dispatch` function L39-48 — `( &mut self, pg: impl FnOnce(&mut PgConnection) -> R, sqlite: impl FnOnce(&mut S...` — Run a per-backend closure on the active connection arm.
-  `DualConnection` type L31-49 — `= DualConnection` — ```

#### src/lib.rs

- pub `backend` module L20 — `-` — ```
- pub `escape` module L21 — `-` — ```
- pub `pool` module L22 — `-` — ```
- pub `sql_types` module L23 — `-` — ```
- pub `types` module L24 — `-` — ```
- pub `DualConnection` enum L48-53 — `Pg | Sqlite` — The canonical dual-backend connection.

#### src/pool.rs

- pub `Backend` enum L25-28 — `Postgres | Sqlite` — Which backend a URL refers to.
- pub `detect_backend` function L35-46 — `(url: &str) -> Option<Backend>` — Detect the backend from a database URL.
- pub `DualConnectionManager` struct L50-53 — `{ url: String, backend: Backend }` — An r2d2 connection manager that establishes a fixed backend.
- pub `new` function L57-63 — `(url: &str) -> Result<Self, Error>` — Build a manager, detecting the backend from `url`.
- pub `Pool` struct L96 — `-` — A pool of [`DualConnection`]s.
- pub `PooledConnection` type L99 — `= diesel::r2d2::PooledConnection<DualConnectionManager>` — A pooled connection — derefs to [`DualConnection`].
- pub `connect` function L103-105 — `(url: &str) -> Result<Self, Error>` — Detect the backend from `url` and build a pool with default settings.
- pub `builder` function L108-112 — `() -> Builder` — Start configuring a pool (`max_size`, timeouts, …) before connecting.
- pub `get` function L115-117 — `(&self) -> Result<PooledConnection, Error>` — Check out a pooled connection.
- pub `inner` function L120-122 — `(&self) -> &diesel::r2d2::Pool<DualConnectionManager>` — The underlying r2d2 pool, for knobs not surfaced here.
- pub `Builder` struct L127-129 — `{ inner: diesel::r2d2::Builder<DualConnectionManager> }` — Builder for [`Pool`].
- pub `max_size` function L133-136 — `(mut self, n: u32) -> Self` — Maximum number of pooled connections.
- pub `min_idle` function L139-142 — `(mut self, n: Option<u32>) -> Self` — Minimum idle connections to maintain.
- pub `connection_timeout` function L145-148 — `(mut self, t: std::time::Duration) -> Self` — Maximum time to wait for a connection on checkout before erroring.
- pub `connect` function L152-155 — `(self, url: &str) -> Result<Pool, Error>` — Detect the backend from `url` and build the pool, eagerly opening the
- pub `connect_lazy` function L162-165 — `(self, url: &str) -> Result<Pool, Error>` — Detect the backend from `url` and build the pool **lazily**: no
- pub `Error` enum L170-175 — `UnknownUrl | Pool` — Errors from [`Pool`] construction and checkout.
-  `DualConnectionManager` type L55-64 — `= DualConnectionManager` — SQLite), the pool establishes exactly the detected backend.
-  `DualConnectionManager` type L66-92 — `impl ManageConnection for DualConnectionManager` — SQLite), the pool establishes exactly the detected backend.
-  `Connection` type L67 — `= DualConnection` — SQLite), the pool establishes exactly the detected backend.
-  `Error` type L68 — `= diesel::r2d2::Error` — SQLite), the pool establishes exactly the detected backend.
-  `connect` function L70-83 — `(&self) -> Result<DualConnection, Self::Error>` — SQLite), the pool establishes exactly the detected backend.
-  `is_valid` function L85-87 — `(&self, conn: &mut DualConnection) -> Result<(), Self::Error>` — SQLite), the pool establishes exactly the detected backend.
-  `has_broken` function L89-91 — `(&self, conn: &mut DualConnection) -> bool` — SQLite), the pool establishes exactly the detected backend.
-  `Pool` type L101-123 — `= Pool` — SQLite), the pool establishes exactly the detected backend.
-  `Builder` type L131-166 — `= Builder` — SQLite), the pool establishes exactly the detected backend.
-  `Error` type L177-186 — `= Error` — SQLite), the pool establishes exactly the detected backend.
-  `fmt` function L178-185 — `(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result` — SQLite), the pool establishes exactly the detected backend.
-  `Error` type L188-195 — `= Error` — SQLite), the pool establishes exactly the detected backend.
-  `source` function L189-194 — `(&self) -> Option<&(dyn std::error::Error + 'static)>` — SQLite), the pool establishes exactly the detected backend.
-  `tests` module L198-231 — `-` — SQLite), the pool establishes exactly the detected backend.
-  `detects_postgres` function L202-208 — `()` — SQLite), the pool establishes exactly the detected backend.
-  `detects_sqlite` function L211-223 — `()` — SQLite), the pool establishes exactly the detected backend.
-  `rejects_unknown_scheme` function L226-230 — `()` — SQLite), the pool establishes exactly the detected backend.

#### src/sql_types.rs

- pub `Uuid` struct L16 — `-` — Portable UUID.
- pub `Bytes` struct L25 — `-` — Portable binary blob.
- pub `Timestamp` struct L36 — `-` — Portable UTC timestamp.
- pub `Json` struct L62 — `-` — Portable JSON.
- pub `Decimal` struct L74 — `-` — Portable arbitrary-precision decimal.
- pub `Array` struct L115 — `-` — Portable array of `ST`.
-  `Timestamp` type L43-46 — `= Timestamp` — against a single logical type that resolves correctly on either backend.
-  `Rhs` type L44 — `= diesel::sql_types::Interval` — against a single logical type that resolves correctly on either backend.
-  `Output` type L45 — `= Timestamp` — against a single logical type that resolves correctly on either backend.
-  `Timestamp` type L49-52 — `= Timestamp` — against a single logical type that resolves correctly on either backend.
-  `Rhs` type L50 — `= diesel::sql_types::Interval` — against a single logical type that resolves correctly on either backend.
-  `Output` type L51 — `= Timestamp` — against a single logical type that resolves correctly on either backend.
-  `Decimal` type L81-84 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `Rhs` type L82 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `Output` type L83 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `Decimal` type L87-90 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `Rhs` type L88 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `Output` type L89 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `Decimal` type L93-96 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `Rhs` type L94 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `Output` type L95 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `Decimal` type L99-102 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `Rhs` type L100 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `Output` type L101 — `= Decimal` — against a single logical type that resolves correctly on either backend.
-  `default` function L121-123 — `() -> Self` — against a single logical type that resolves correctly on either backend.

### src/types

> *Semantic summary to be generated by AI agent.*

#### src/types/array.rs

- pub `Array` struct L30 — `-` — A portable array.
-  `from` function L33-35 — `(value: Vec<T>) -> Self` — newtype can.
-  `from` function L39-41 — `(value: Array<T>) -> Self` — newtype can.
-  `Pg` type L46-53 — `= Pg` — newtype can.
-  `metadata` function L50-52 — `(lookup: &mut Self::MetadataLookup) -> Self::TypeMetadata` — newtype can.
-  `Sqlite` type L55-59 — `= Sqlite` — newtype can.
-  `metadata` function L56-58 — `(lookup: &mut Self::MetadataLookup) -> Self::TypeMetadata` — newtype can.
-  `MultiBackend` type L61-68 — `= MultiBackend` — newtype can.
-  `metadata` function L65-67 — `(lookup: &mut Self::MetadataLookup) -> Self::TypeMetadata` — newtype can.
-  `to_sql` function L77-79 — `(&'b self, out: &mut Output<'b, '_, Pg>) -> serialize::Result` — newtype can.
-  `from_sql` function L86-88 — `(bytes: PgValue<'_>) -> deserialize::Result<Self>` — newtype can.
-  `to_sql` function L97-100 — `(&'b self, out: &mut Output<'b, '_, Sqlite>) -> serialize::Result` — newtype can.
-  `from_sql` function L107-110 — `(bytes: SqliteValue<'_, '_, '_>) -> deserialize::Result<Self>` — newtype can.
-  `to_sql` function L127-130 — `(&'b self, out: &mut Output<'b, '_, MultiBackend>) -> serialize::Result` — newtype can.
-  `from_sql` function L138-140 — `(bytes: <MultiBackend as Backend>::RawValue<'_>) -> deserialize::Result<Self>` — newtype can.
-  `Expression` type L149 — `= Bound<sql_types::Array<ST>, Self>` — newtype can.
-  `as_expression` function L150-152 — `(self) -> Self::Expression` — newtype can.
-  `Expression` type L159 — `= Bound<Nullable<sql_types::Array<ST>>, Self>` — newtype can.
-  `as_expression` function L160-162 — `(self) -> Self::Expression` — newtype can.
-  `Expression` type L169 — `= Bound<sql_types::Array<ST>, Self>` — newtype can.
-  `as_expression` function L170-172 — `(self) -> Self::Expression` — newtype can.
-  `Row` type L181 — `= Self` — newtype can.
-  `build` function L182-184 — `(row: Self::Row) -> deserialize::Result<Self>` — newtype can.

#### src/types/bytes.rs

- pub `Bytes` struct L20 — `-` — A binary blob that serializes portably across PostgreSQL and SQLite.
-  `Bytes` type L22-26 — `= Bytes` — `bytea`, SQLite `BLOB`.
-  `from` function L23-25 — `(value: Vec<u8>) -> Self` — `bytea`, SQLite `BLOB`.
-  `from` function L29-31 — `(value: Bytes) -> Self` — `bytea`, SQLite `BLOB`.
-  `Bytes` type L36-40 — `= Bytes` — `bytea`, SQLite `BLOB`.
-  `to_sql` function L37-39 — `(&'b self, out: &mut Output<'b, '_, Pg>) -> serialize::Result` — `bytea`, SQLite `BLOB`.
-  `Bytes` type L42-46 — `= Bytes` — `bytea`, SQLite `BLOB`.
-  `from_sql` function L43-45 — `(bytes: PgValue<'_>) -> deserialize::Result<Self>` — `bytea`, SQLite `BLOB`.
-  `Bytes` type L50-54 — `= Bytes` — `bytea`, SQLite `BLOB`.
-  `to_sql` function L51-53 — `(&'b self, out: &mut Output<'b, '_, Sqlite>) -> serialize::Result` — `bytea`, SQLite `BLOB`.
-  `Bytes` type L56-60 — `= Bytes` — `bytea`, SQLite `BLOB`.
-  `from_sql` function L57-59 — `(bytes: SqliteValue<'_, '_, '_>) -> deserialize::Result<Self>` — `bytea`, SQLite `BLOB`.

#### src/types/decimal.rs

- pub `Decimal` struct L25 — `-` — An arbitrary-precision decimal that serializes portably across PostgreSQL and
-  `Decimal` type L27-31 — `= Decimal` — that text is not guaranteed; do decimal math in Rust.
-  `from` function L28-30 — `(value: BigDecimal) -> Self` — that text is not guaranteed; do decimal math in Rust.
-  `BigDecimal` type L33-37 — `= BigDecimal` — that text is not guaranteed; do decimal math in Rust.
-  `from` function L34-36 — `(value: Decimal) -> Self` — that text is not guaranteed; do decimal math in Rust.
-  `Decimal` type L41-45 — `= Decimal` — that text is not guaranteed; do decimal math in Rust.
-  `to_sql` function L42-44 — `(&'b self, out: &mut Output<'b, '_, Pg>) -> serialize::Result` — that text is not guaranteed; do decimal math in Rust.
-  `Decimal` type L47-51 — `= Decimal` — that text is not guaranteed; do decimal math in Rust.
-  `from_sql` function L48-50 — `(bytes: PgValue<'_>) -> deserialize::Result<Self>` — that text is not guaranteed; do decimal math in Rust.
-  `Decimal` type L55-60 — `= Decimal` — that text is not guaranteed; do decimal math in Rust.
-  `to_sql` function L56-59 — `(&'b self, out: &mut Output<'b, '_, Sqlite>) -> serialize::Result` — that text is not guaranteed; do decimal math in Rust.
-  `Decimal` type L62-68 — `= Decimal` — that text is not guaranteed; do decimal math in Rust.
-  `from_sql` function L63-67 — `(bytes: SqliteValue<'_, '_, '_>) -> deserialize::Result<Self>` — that text is not guaranteed; do decimal math in Rust.

#### src/types/json.rs

- pub `Json` struct L27 — `-` — A JSON value that serializes portably across PostgreSQL and SQLite.
-  `from` function L30-32 — `(value: T) -> Self` — keep TEXT for portability and debuggability.
-  `to_sql` function L41-45 — `(&'b self, out: &mut Output<'b, '_, Pg>) -> serialize::Result` — keep TEXT for portability and debuggability.
-  `from_sql` function L52-58 — `(value: PgValue<'_>) -> deserialize::Result<Self>` — keep TEXT for portability and debuggability.
-  `to_sql` function L67-70 — `(&'b self, out: &mut Output<'b, '_, Sqlite>) -> serialize::Result` — keep TEXT for portability and debuggability.
-  `from_sql` function L77-80 — `(bytes: SqliteValue<'_, '_, '_>) -> deserialize::Result<Self>` — keep TEXT for portability and debuggability.

#### src/types/mod.rs

-  `array` module L9 — `-` — Each type wraps a Rust value and carries `ToSql`/`FromSql` impls against
-  `bytes` module L10 — `-` — [`crate::backend`] and is added in DDB-T-0003.
-  `decimal` module L12 — `-` — [`crate::backend`] and is added in DDB-T-0003.
-  `json` module L14 — `-` — [`crate::backend`] and is added in DDB-T-0003.
-  `timestamp` module L16 — `-` — [`crate::backend`] and is added in DDB-T-0003.
-  `uuid` module L18 — `-` — [`crate::backend`] and is added in DDB-T-0003.

#### src/types/timestamp.rs

- pub `Timestamp` struct L31 — `-` — A UTC timestamp that serializes portably across PostgreSQL and SQLite.
-  `SQLITE_FORMAT` variable L23 — `: &str` — Canonical SQLite text format (ADR DDB-A-0001).
-  `Timestamp` type L33-37 — `= Timestamp` — precision; finer precision is truncated.
-  `from` function L34-36 — `(value: DateTime<Utc>) -> Self` — precision; finer precision is truncated.
-  `from` function L40-42 — `(value: Timestamp) -> Self` — precision; finer precision is truncated.
-  `Timestamp` type L47-51 — `= Timestamp` — precision; finer precision is truncated.
-  `to_sql` function L48-50 — `(&'b self, out: &mut Output<'b, '_, Pg>) -> serialize::Result` — precision; finer precision is truncated.
-  `Timestamp` type L53-57 — `= Timestamp` — precision; finer precision is truncated.
-  `from_sql` function L54-56 — `(bytes: PgValue<'_>) -> deserialize::Result<Self>` — precision; finer precision is truncated.
-  `Timestamp` type L61-66 — `= Timestamp` — precision; finer precision is truncated.
-  `to_sql` function L62-65 — `(&'b self, out: &mut Output<'b, '_, Sqlite>) -> serialize::Result` — precision; finer precision is truncated.
-  `Timestamp` type L68-76 — `= Timestamp` — precision; finer precision is truncated.
-  `from_sql` function L69-75 — `(bytes: SqliteValue<'_, '_, '_>) -> deserialize::Result<Self>` — precision; finer precision is truncated.

#### src/types/uuid.rs

- pub `Uuid` struct L20 — `-` — A UUID that serializes portably across PostgreSQL and SQLite.
-  `Uuid` type L22-26 — `= Uuid` — 16 bytes as a `BLOB`.
-  `from` function L23-25 — `(value: uuid::Uuid) -> Self` — 16 bytes as a `BLOB`.
-  `from` function L29-31 — `(value: Uuid) -> Self` — 16 bytes as a `BLOB`.
-  `Uuid` type L36-40 — `= Uuid` — 16 bytes as a `BLOB`.
-  `to_sql` function L37-39 — `(&'b self, out: &mut Output<'b, '_, Pg>) -> serialize::Result` — 16 bytes as a `BLOB`.
-  `Uuid` type L42-46 — `= Uuid` — 16 bytes as a `BLOB`.
-  `from_sql` function L43-45 — `(bytes: PgValue<'_>) -> deserialize::Result<Self>` — 16 bytes as a `BLOB`.
-  `Uuid` type L50-55 — `= Uuid` — 16 bytes as a `BLOB`.
-  `to_sql` function L51-54 — `(&'b self, out: &mut Output<'b, '_, Sqlite>) -> serialize::Result` — 16 bytes as a `BLOB`.
-  `Uuid` type L57-64 — `= Uuid` — 16 bytes as a `BLOB`.
-  `from_sql` function L58-63 — `(bytes: SqliteValue<'_, '_, '_>) -> deserialize::Result<Self>` — 16 bytes as a `BLOB`.

### tests

> *Semantic summary to be generated by AI agent.*

#### tests/array.rs

-  `create_table` function L20-36 — `(conn: &mut DualConnection)` — `Array` round-trips on both backends (PG native `T[]`, SQLite JSON TEXT).
-  `array_round_trips` function L39-78 — `(conn: &mut DualConnection)` — `Array` round-trips on both backends (PG native `T[]`, SQLite JSON TEXT).

#### tests/bytes.rs

-  `CREATE_SQLITE` variable L24-25 — `: &str` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.
-  `CREATE_PG` variable L26-27 — `: &str` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.
-  `create_table` function L31-37 — `(conn: &mut DualConnection)` — The schema is the one legitimately per-backend bit (BLOB vs BYTEA); match on
-  `bytes_round_trips` function L40-66 — `(conn: &mut DualConnection)` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.
-  `bytes_empty_vs_null` function L69-106 — `(conn: &mut DualConnection)` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.

#### tests/connectivity.rs

-  `sqlite_in_memory_connects` function L13-18 — `()` — `#[dualdb::test]` harness will later formalize.
-  `postgres_connects_when_configured` function L21-30 — `()` — `#[dualdb::test]` harness will later formalize.

#### tests/decimal.rs

-  `create_table` function L22-32 — `(conn: &mut DualConnection)` — `Decimal` round-trips on both backends (PG native `numeric`, SQLite TEXT).
-  `decimal_round_trips` function L35-63 — `(conn: &mut DualConnection)` — `Decimal` round-trips on both backends (PG native `numeric`, SQLite TEXT).

#### tests/enum.rs

- pub `Mood` enum L10-15 — `Happy | Sad | Neutral` — (PostgreSQL native `enum`, SQLite `TEXT`).
-  `create_schema` function L27-42 — `(conn: &mut DualConnection)` — (PostgreSQL native `enum`, SQLite `TEXT`).
-  `enum_round_trips` function L45-68 — `(conn: &mut DualConnection)` — (PostgreSQL native `enum`, SQLite `TEXT`).

#### tests/generated.rs

-  `PG_UP` variable L22-23 — `: &str` — marker).
-  `SQLITE_UP` variable L24-25 — `: &str` — marker).
-  `apply_init` function L27-33 — `(conn: &mut DualConnection)` — marker).
-  `generated_schema_round_trips` function L36-77 — `(conn: &mut DualConnection)` — marker).

#### tests/json.rs

-  `Doc` struct L26-30 — `{ name: String, tags: Vec<String>, count: i64 }` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.
-  `create_table` function L32-44 — `(conn: &mut DualConnection)` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.
-  `json_value_round_trips` function L47-72 — `(conn: &mut DualConnection)` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.
-  `json_typed_struct_round_trips` function L75-93 — `(conn: &mut DualConnection)` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.

#### tests/pool.rs

-  `bridge_via_pooled` function L29-42 — `(conn: &mut DualConnection, create_sql: &str)` — explicit URL/scheme detection added in M2.
-  `r2d2_pool_over_dualconnection_sqlite` function L45-57 — `()` — explicit URL/scheme detection added in M2.
-  `r2d2_pool_over_dualconnection_postgres` function L60-75 — `()` — explicit URL/scheme detection added in M2.
-  `dualdb_pool_connect_sqlite` function L78-89 — `()` — explicit URL/scheme detection added in M2.
-  `dualdb_pool_connect_postgres` function L92-104 — `()` — explicit URL/scheme detection added in M2.

#### tests/timestamp.rs

-  `create_table` function L24-36 — `(conn: &mut DualConnection)` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.
-  `cases` function L40-47 — `() -> Vec<DateTime<Utc>>` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.
-  `timestamp_round_trips` function L50-78 — `(conn: &mut DualConnection)` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.
-  `timestamp_truncates_to_micros` function L81-102 — `(conn: &mut DualConnection)` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.

#### tests/upsert.rs

-  `create_table` function L23-33 — `(conn: &mut DualConnection)` — This test proves the per-backend path works on both.
-  `value` function L35-41 — `(conn: &mut DualConnection) -> i32` — This test proves the per-backend path works on both.
-  `upsert_set_v` macro L44-54 — `-` — This test proves the per-backend path works on both.
-  `upsert_do_nothing` macro L56-65 — `-` — This test proves the per-backend path works on both.
-  `upsert_via_escape_hatch` function L68-99 — `(conn: &mut DualConnection)` — This test proves the per-backend path works on both.
-  `upsert_via_dispatch` function L104-113 — `(conn: &mut DualConnection)` — The same upsert, written with the `dispatch` escape-hatch helper instead of a

#### tests/uuid.rs

-  `Item` struct L27-31 — `{ id: Uuid, name: String, count: i32 }` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.
-  `create_table` function L34-46 — `(conn: &mut DualConnection)` — Per-backend DDL (UUID vs BLOB) — the one legitimately divergent bit.
-  `uuid_round_trips` function L49-76 — `(conn: &mut DualConnection)` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.
-  `uuid_insert_returning` function L79-100 — `(conn: &mut DualConnection)` — SQLite runs in-memory; Postgres runs only when `DUALDB_PG_URL` is set.

